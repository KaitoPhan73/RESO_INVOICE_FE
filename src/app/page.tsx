import { TableColumnsType } from "antd";
import Image from "next/image";
interface UserData {
  id: string;
  name: string;
  avatar: string;
  role: string;
}
export default function Home(props: any) {
  return <div>Home</div>;
}
